<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".view.map.DriverMapActivity">

    <android.support.v7.widget.CardView
        android:layout_marginTop="20dp"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay"
        app:cardBackgroundColor="@color/colorBlackLucid"
        app:cardElevation="8dp"
        app:cardCornerRadius="6dp">
        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:popupTheme="@style/AppTheme.PopupOverlay"/>
    </android.support.v7.widget.CardView>
    <!-- Content Activity -->
    <include layout="@layout/content_driver_map" />
    <!-- Bottom Sheet -->
    <include layout="@layout/bottom_sheet_petition" />
    <!-- Fab Button -->
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_my_position"
        app:backgroundTint="@color/colorPrimaryDark"
        app:fabSize="normal"
        app:layout_anchor="@id/layout_sheet"
        app:layout_anchorGravity="top|end"
        android:layout_margin="@dimen/fab_margin"
        android:src="@drawable/ic_gps"
        android:scaleType="centerInside"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end" />

</android.support.design.widget.CoordinatorLayout>